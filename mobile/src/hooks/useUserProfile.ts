/**
 * User profile hook - thin wrapper around UserProfileContext
 * Use this in screens to access profile state
 */

export { useUserProfileContext as useUserProfile } from "../contexts/UserProfileContext";
